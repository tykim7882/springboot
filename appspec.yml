version: 0.0 # CodeDeploy 버전
os: linux
files:
  - source: / # CodeDeploy에 전달해준 파일 중 destination로 이동시킬 대상 (루트지정으로 파일 전체를 대상으로 함)
    destination: /home/ec2-user/app/step2/zip # 이후 Jar 실행은 destination 에서 옮긴 파일로 진행됨
    overwrite: yes
